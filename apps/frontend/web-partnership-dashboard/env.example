# Copy this to .env or .env.local to configure your environment

# Supabase Configuration (set in .env.local for dev, in Vercel/GitHub for prod)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Map provider keys (optional)
# VITE_MAPTILER_KEY=your_maptiler_key
# VITE_MAP_STYLE_URL=your_custom_style_url

# Optional: Service role key for admin operations (never commit real value)
# VITE_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here