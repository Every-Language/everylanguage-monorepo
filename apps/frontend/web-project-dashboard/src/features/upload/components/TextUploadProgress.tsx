/**
 * @deprecated Text upload progress is handled differently now.
 * This component is kept for compatibility but is currently a no-op.
 */
export function TextUploadProgress() {
  // No-op: Text uploads use a different flow now
  return null;
} 