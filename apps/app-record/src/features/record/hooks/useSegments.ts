/**
 * Hook for fetching segments for a sequence
 *
 * @deprecated Use useGetSegments from '../api/get-segments' instead.
 * This hook is kept for backward compatibility but will be removed in a future version.
 */
export { useGetSegments as useSegments } from '../api/get-segments';
