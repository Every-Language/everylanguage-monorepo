/**
 * Hook for fetching a single sequence by ID
 *
 * @deprecated Use useGetSequence from '../api/get-sequence' instead.
 * This hook is kept for backward compatibility but will be removed in a future version.
 */
export { useGetSequence as useSequence } from '../api/get-sequence';
